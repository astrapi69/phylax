/**
 * First-run entry screen placeholder. ONB-01b replaces this stub with
 * the real welcome view (logo, tagline, trust signals, CTAs to
 * /privacy and /backup/import/select).
 */
export function WelcomeView() {
  return (
    <div data-testid="welcome-stub" className="p-8">
      Welcome (ONB-01b placeholder)
    </div>
  );
}
