/**
 * First-run vault setup placeholder. ONB-01c replaces this stub with
 * the real setup view (zxcvbn strength indicator, confirmation, warning
 * block + acknowledgment, submit wires to useSetupVault).
 */
export function SetupView() {
  return (
    <div data-testid="setup-stub" className="p-8">
      Setup (ONB-01c placeholder)
    </div>
  );
}
