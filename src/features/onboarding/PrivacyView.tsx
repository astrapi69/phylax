/**
 * First-run privacy disclosure placeholder. ONB-01b replaces this stub
 * with the real privacy view (three paragraphs + primary / back CTAs).
 */
export function PrivacyView() {
  return (
    <div data-testid="privacy-stub" className="p-8">
      Privacy (ONB-01b placeholder)
    </div>
  );
}
