/**
 * Backup import decryption placeholder. ONB-01e replaces this stub with
 * the real view (password prompt, PBKDF2 derive, AES-GCM decrypt of the
 * selected .phylax backup, move data into IndexedDB, redirect to /app).
 */
export function BackupImportUnlockView() {
  return (
    <div data-testid="backup-import-unlock-stub" className="p-8">
      Backup import: unlock (ONB-01e placeholder)
    </div>
  );
}
