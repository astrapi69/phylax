/**
 * Backup import file-selection placeholder. ONB-01e replaces this stub
 * with the real view (file picker for .phylax backups, metadata display,
 * continue CTA to /backup/import/unlock).
 *
 * Distinct from the Markdown profile import flow at /import (which
 * handles Living Health Markdown profiles, not encrypted backups).
 */
export function BackupImportSelectView() {
  return (
    <div data-testid="backup-import-select-stub" className="p-8">
      Backup import: select (ONB-01e placeholder)
    </div>
  );
}
